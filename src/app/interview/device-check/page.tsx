"use client";

// ============================================================================
// /interview/device-check — Sonru-style device diagnostics route.
// Renders the shared DeviceCheck component built on the Phase 1 media hooks.
// ============================================================================

import { DeviceCheck } from "@/components/interview/DeviceCheck";

export default function DeviceCheckPage() {
  return <DeviceCheck />;
}
